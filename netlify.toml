[build]
  publish = "."
  functions = "netlify/functions"

# Set up clean redirects for local development or neat API routes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
